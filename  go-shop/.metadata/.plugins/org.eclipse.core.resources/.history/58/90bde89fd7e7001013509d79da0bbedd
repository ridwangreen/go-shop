package GUI;

import java.awt.Color;

import javax.swing.ImageIcon;

public class PegColor extends ImageIcon{
	
	public PegColor(Color color){
		
		if(color.equals(Color.GRAY)){
			setImage(rcs)
		}
	}
}
