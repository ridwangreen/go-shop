package GUI;

import java.awt.GridLayout;

import javax.swing.JButton;
import javax.swing.JPanel;

public class BottomPanel extends JPanel{
	
	private JButton makeGuess;
	private JButton leaveFeedback;
	private JButton undo;
	private JButton setCode;
	private JButton bottomButtons[];

	public BottomPanel() {

		bottomButtons = new JButton[4];
		setLayout(new GridLayout(1, 4));
		makeGuess = new JButton("Make Guess");
		bottomButtons[0] = makeGuess;
		leaveFeedback = new JButton("Leave Feedback");
		bottomButtons[1] = leaveFeedback;
		undo = new JButton("Undo Move");
		bottomButtons[2] = undo;
		setCode = new JButton("Set Code");
		bottomButtons[3] = setCode;
		add(makeGuess);
		add(leaveFeedback);
		add(undo);
		add(setCode);
		setVisible(true);
	}
	
	public void setBottomButtonEnabled(int buttonNumber, boolean isEnabled){
		
		bottomButtons[buttonNumber].setEnabled(isEnabled);
	}
}
