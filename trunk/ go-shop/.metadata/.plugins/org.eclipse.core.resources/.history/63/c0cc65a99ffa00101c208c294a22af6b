package Command;

import player.CodeBreaker_AI;
import player.CodeMaker;
import player.Difficulty;
import system.GameState;
import system.LogHandler;
import system.Peg;

public class NewGame extends Command{

	public void execute() {
		// TODO add AI/Player dialog
		boolean isPlayer = false;
		
		if(isPlayer==false){
			// TODO add difficulty dialog
			CodeBreaker_AI ai = new CodeBreaker_AI();
			GameState.getInstance().addObserver(ai);
		}
		
		// TODO add AI/Player dialog
		CodeMaker maker = new CodeMaker();
		GameState.getInstance().addObserver(maker);

	}

	public void undo() {
	}
}
